<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 style="font-weight: 100">Colecciones de viaje</h1>
        </div>
        <div class="col-md-12">
            <form action="/place/collections" method="POST">
                <div class="form-group">
                    <label for="destination-input">Destino:</label>
                    <input name="destination" type="text" class="form-control" id="destination-input">
                </div>
                <div class="form-group">
                    <label for="description-input">Descripción</label>
                    <input name="description" type="text" class="form-control" id="description-input">
                </div>
                <div class="form-group">
                    <label for="image">Imagen:</label>
                </div><br>
                <button type="submit" class="btn btn-dark">Crear Viaje!</button>
            </form>
        </div>
  <p>Listas de viajes:</p>
  <div class="container">
  <h1>Beers</h1>
 <div class="row">
        {{#each allPlaces}}
        <div class="col-md-4">
            <div class="card">
                <img class="card-img-top" src="imagePlaceUr" alt="image-destination">
                <div class="card-body">
                    <h5 class="card-title">{{{destination}}}</h5>
                    <small>{{{tagline}}}</small>
                    <p class="card-text">{{{description}}} </p>
                    <a href="/place/marker/edit/{{id}}" class="btn btn-outline-dark me-2">Edita</a>
                    <a href="#" class="btn btn-outline-dark me-2">Borrar</a>
                </div>
            </div>
        </div>
      {{/each}}   
</div>
</div>

